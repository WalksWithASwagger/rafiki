# Copy to .env in this directory (same folder as index.js). Never commit .env.

GOOGLE_API_KEY=

# Optional: Chrome/Chromium for `npx rafiki --render` (see docs/CHROME-PUPPETEER.md)
# PUPPETEER_EXECUTABLE_PATH=
