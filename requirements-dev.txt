# Cross-platform development source requirements. Install with:
#   .venv/bin/python -m pip install -r requirements-dev.txt
pytest>=9.1.1
pytest-mock>=3.15.1
jsonschema>=4.26.0
pip-audit>=2.10.1
pip-tools==7.6.1  # Pin the compiler so lock regeneration is stable.
ruff>=0.15
