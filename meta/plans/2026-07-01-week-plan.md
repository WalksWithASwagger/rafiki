# Rafiki Week Plan — 2026-07-01 → 2026-07-05

> Dated planning snapshot. The live roadmap is [docs/ROADMAP.md](../../docs/ROADMAP.md);
> this plan sequences a single week against the currently-open issues.

## Where things stand (2026-07-01)

- **Docs/structure audit shipped** — PR #284 (draft, CI green): archived dated
  snapshots into `meta/audits/`, fixed stale refs (FOLDER-LAYOUT phantom docs,
  portal port, gitleaks entry, MCP "proposed" wording), removed dead scripts,
  consolidated issue templates. Roadmap statuses + Floyo system-map row updated.
- **Active thrust:** epic **#268** — retire the `alex-samuel` studio into Rafiki
  (three-way split: rafiki = tool, `gorgeous-ghost` = private film work,
  `alex-samuel` = archived).
- **12 open issues:** #263, #264, #265, #266, #267, #268, #270, #271, #274 (the
  epic + tracks) and #69, #200, #201 (standalone/human-gated).

## Priorities for the week

Ordered by value and by what an agent can land safely (dry-run-first, no
operator spend, no credentials).

### P0 — Land PR #284 and finish Floyo Track 1
1. **Merge PR #284** once reviewed (docs audit; CI green). Small, unblocks a
   clean base.
2. **#263 Floyo Phase 2 tail — plate/faceswap resolver.** Last piece of Track 1:
   the ~source→swapped plate convention (no engine). Dry-run-first + tests.
   Pattern: `lib/providers/floyo_provider.py`, `lib/floyo_jobs.py`.

### P1 — Highest-value review UX
3. **#271 Track 3 — clips review surface in the portal.** One-at-a-time
   playback, audio badges (🔊/🔇), ❤/✓/✕ + notes, byte-range seek, filters.
   Byte-range video already supported in `lib/server.py`; state persisted as
   JSON like ratings/feedback. Agent-buildable, no spend.

### P2 — Small, self-contained importer + planning
4. **#265 Track 5a — production-knowledge indexing (size S).** Teach the
   importer to recognize `keyframes.json` / `style_lora.json` manifests and
   index production `.md` docs. Fixtures + tests in
   `lib/importers/alex_samuel.py`. Small and agent-suitable.
5. **#266 Track 5b — media home decision (size M, planning).** Decide the
   canonical gitignored local media-root path + migration approach (move vs
   re-point indexer) that keeps `check-public-boundary.py` green. Write the
   decision; defer the actual ~14 GB migration to an operator session.

### Human-gated — tee up, don't block on
6. **#200 Curriculum Atlas story-rail validation (needs-human).** Prep a review
   script/checklist so KK can run one Teach-mode session; capture notes that
   unblock #201 export controls.
7. **#69 Notion live signed-upload verification (needs-human, credentials).**
   Only actionable with throwaway Notion creds; leave dry-run/mocked path as-is
   until then.

## Suggested sequencing

| Day | Focus |
|---|---|
| Wed 07-01 | Merge #284; scope + start #263 faceswap resolver (dry-run + tests). |
| Thu 07-02 | Finish #263 resolver; open PR. Begin #271 clips surface (server + data). |
| Fri 07-03 | #271 clips surface UI + E2E assertion; open PR. |
| Weekend (opt.) | #265 importer indexing (small); draft #266 media-home decision memo. |

## Dependencies & gates

- **Track 6 cutover (#267)** stays blocked until Tracks 1–5 close and a real
  Andromeda flow is reproduced in Rafiki end-to-end.
- **Track 7 Phase B (#274)** media migration is gated on Floyo parity (Track 1).
- Every PR must pass the roadmap verification gates: `npm test`,
  `npm run pack:check`, `npm run public:check`, `npm run doctor`,
  `npm run smoke:dry-run`, `npm run e2e:portal`, `npm run docs:check`.
- Hard rule for the whole epic: port **tooling** into `lib/`; private media and
  absolute private paths never enter git (`check-public-boundary.py` green).

## Explicitly not this week

- Retirement cutover / archiving `alex-samuel` (#267) — blocked.
- The ~14 GB corpus move (#266 execution) — operator session, not agent work.
- LoRA training runs (#270) and any Floyo `--execute` runs — operator-gated spend.
